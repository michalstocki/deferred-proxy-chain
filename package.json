{
  "name": "deferred-proxy-chain",
  "version": "0.0.2",
  "description": "Allows collecting chained calls that are evaluated asynchronously when the target object arrives",
  "main": "src/DeferredChain.js",
  "types": "src/DeferredChain.d.ts",
  "scripts": {
    "test": "jest",
    "prepublish": "tsc"
  },
  "engines": {
    "node": ">=6.5"
  },
  "keywords": [
    "chain",
    "proxy",
    "deferred",
    "lazy",
    "calls",
    "object"
  ],
  "author": "Michał Stocki",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^22.1.3",
    "jest": "^22.4.2",
    "ts-jest": "^22.4.0",
    "tslint": "^5.9.1",
    "tslint-config-uxpin": "^1.5.0",
    "typescript": "^2.7.2"
  },
  "dependencies": {
  }
}
