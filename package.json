{
  "name": "deferred-proxy-chain",
  "version": "1.0.0",
  "description": "Allows collecting chained calls that are evaluated asynchronously when the target object arrives",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chain",
    "proxy",
    "deferred",
    "lazy",
    "calls",
    "object"
  ],
  "author": "Michał Stocki",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^22.1.3",
    "jest": "^22.4.2",
    "ts-jest": "^22.4.0",
    "typescript": "^2.7.2"
  }
}
